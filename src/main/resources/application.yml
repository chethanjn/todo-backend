server:
  port: ${PORT:8080}

spring:
  application:
    name: todo-backend

  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI}

app:
  jwt:
    secret: ${JWT_SECRET:VGhpcy1pcy1hLWRldmVsb3BtZW50LXNlY3JldC1iYXNlNjQtMzItYnl0ZXM=}
    expiration-ms: 86400000

logging:
  level:
    org.springframework.security: INFO
    org.springframework.web: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
